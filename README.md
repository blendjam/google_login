# Google Sign In extension for Defold
Just add `web_client_id` to your `game.project` file. The `web_client_id` is the the Client ID of you Web Application. Need to add `ios_client_id` for ios devices

You can learn more in the [official manual](https://developer.android.com/identity/sign-in/credential-manager-siwg).
